# Logs
*.log

# OS
.DS_Store
Thumbs.db

# Environment
.env
.env.backup
.env.production
!.env.example

# Security — never commit keys or certificates
deploy-key-*
auth.json
*.pem
*.p12
*.crt
*.cert
storage/*.key

# Laravel
/vendor/
/node_modules/
/public/build
/public/hot
/public/storage
/public/fonts-manifest.dev.json
/storage/pail

# Testing
.phpunit.result.cache
/.phpunit.cache

# IDE / Editors
/.idea
/.vscode
/.cursor/
/.nova
/.zed
/.codex
.phpactor.json
_ide_helper.php

# Local dev helpers
Homestead.json
Homestead.yaml

# Scribe generated API docs (regenerate with: php artisan scribe:generate)
/public/docs

# Docker local overrides (machine-specific tweaks only)
docker-compose.override.yml

# Next.js (if frontend is in this repo)
/.next/
/out/

# Old v1 reference code — never commit (contains deploy keys)
old/
